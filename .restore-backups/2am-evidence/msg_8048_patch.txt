{"success": true, "diff": "--- a//home/spatula/Projects/ZenNew/agent-os.html\n+++ b//home/spatula/Projects/ZenNew/agent-os.html\n@@ -779,7 +779,32 @@\n     const maxTok = Math.max(1, ...days.map(day => day.total || 0), ...days.flatMap(day => Object.values(day.models || {})));\n     const x = i => padL + (days.length <= 1 ? 0 : i * ((W - padL - padR) / (days.length - 1)));\n     const y = v => padT + (H - padT - padB) * (1 - (Number(v || 0) / maxTok));\n-    const linePath = vals => vals.map((v,i)=>`${i?'L':'M'} ${x(i).toFixed(1)} ${y(v).toFixed(1)}`).join(' ');\n+    const linePath = vals => {\n+      const pts = vals.map((v,i)=>({x:x(i), y:y(v)}));\n+      if (!pts.length) return '';\n+      if (pts.length === 1) return `M ${pts[0].x.toFixed(1)} ${pts[0].y.toFixed(1)}`;\n+      const ds = [];\n+      for (let i=0; i<pts.length-1; i++) {\n+        ds.push((pts[i+1].y - pts[i].y) / Math.max(1, pts[i+1].x - pts[i].x));\n+      }\n+      const ms = pts.map((_, i) => {\n+        if (i === 0) return ds[0] * 0.65;\n+        if (i === pts.length - 1) return ds[ds.length - 1] * 0.65;\n+        if (ds[i-1] === 0 || ds[i] === 0 || Math.sign(ds[i-1]) !== Math.sign(ds[i])) return 0;\n+        return Math.sign(ds[i]) * Math.min(Math.abs(ds[i-1]), Math.abs(ds[i]), Math.abs((ds[i-1] + ds[i]) / 2)) * 0.65;\n+      });\n+      let dPath = `M ${pts[0].x.toFixed(1)} ${pts[0].y.toFixed(1)}`;\n+      for (let i=0; i<pts.length-1; i++) {\n+        const p0 = pts[i], p1 = pts[i+1];\n+        const dx = p1.x - p0.x;\n+        const c1x = p0.x + dx / 3;\n+        const c1y = p0.y + ms[i] * dx / 3;\n+        const c2x = p1.x - dx / 3;\n+        const c2y = p1.y - ms[i+1] * dx / 3;\n+        dPath += ` C ${c1x.toFixed(1)} ${c1y.toFixed(1)}, ${c2x.toFixed(1)} ${c2y.toFixed(1)}, ${p1.x.toFixed(1)} ${p1.y.toFixed(1)}`;\n+      }\n+      return dPath;\n+    };\n     const totalVals = days.map(day => day.total || 0);\n     const areaPath = totalVals.length ? `${linePath(totalVals)} L ${x(days.length-1).toFixed(1)} ${H-padB} L ${x(0).toFixed(1)} ${H-padB} Z` : '';\n     const gridVals = [0.25,0.5,0.75,1].map(p=>Math.round(maxTok*p));\n", "files_modified": ["/home/spatula/Projects/ZenNew/agent-os.html"], "lint": {"status": "skipped", "message": "No linter for .html files"}, "_warning": "/home/spatula/Projects/ZenNew/agent-os.html was modified since you last read it on disk (external edit or unrecorded writer). Re-read the file before writing.", "resolved_path": "/home/spatula/Projects/ZenNew/agent-os.html"}