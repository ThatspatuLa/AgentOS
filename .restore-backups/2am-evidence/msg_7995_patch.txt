{"success": true, "diff": "--- a//home/spatula/Projects/ZenNew/agent-os.html\n+++ b//home/spatula/Projects/ZenNew/agent-os.html\n@@ -778,21 +778,9 @@\n     const maxTok = Math.max(1, ...days.map(day => day.total || 0), ...days.flatMap(day => Object.values(day.models || {})));\n     const x = i => padL + (days.length <= 1 ? 0 : i * ((W - padL - padR) / (days.length - 1)));\n     const y = v => padT + (H - padT - padB) * (1 - (Number(v || 0) / maxTok));\n-    const pointsFor = vals => vals.map((v,i)=>[x(i), y(v), v]);\n-    const smoothPath = vals => {\n-      const pts = pointsFor(vals);\n-      if (!pts.length) return '';\n-      if (pts.length === 1) return `M ${pts[0][0].toFixed(1)} ${pts[0][1].toFixed(1)}`;\n-      let dPath = `M ${pts[0][0].toFixed(1)} ${pts[0][1].toFixed(1)}`;\n-      for (let i=0; i<pts.length-1; i++) {\n-        const [x0,y0] = pts[i]; const [x1,y1] = pts[i+1];\n-        const cpX = x0 + (x1 - x0) * 0.5;\n-        dPath += ` C ${cpX.toFixed(1)} ${y0.toFixed(1)}, ${cpX.toFixed(1)} ${y1.toFixed(1)}, ${x1.toFixed(1)} ${y1.toFixed(1)}`;\n-      }\n-      return dPath;\n-    };\n+    const linePath = vals => vals.map((v,i)=>`${i?'L':'M'} ${x(i).toFixed(1)} ${y(v).toFixed(1)}`).join(' ');\n     const totalVals = days.map(day => day.total || 0);\n-    const areaPath = totalVals.length ? `${smoothPath(totalVals)} L ${x(days.length-1).toFixed(1)} ${H-padB} L ${x(0).toFixed(1)} ${H-padB} Z` : '';\n+    const areaPath = totalVals.length ? `${linePath(totalVals)} L ${x(days.length-1).toFixed(1)} ${H-padB} L ${x(0).toFixed(1)} ${H-padB} Z` : '';\n     const gridVals = [0.25,0.5,0.75,1].map(p=>Math.round(maxTok*p));\n     const lines = modelsTs.map(model => {\n       const vals = days.map(day => (day.models || {})[model.key] || 0);\n", "files_modified": ["/home/spatula/Projects/ZenNew/agent-os.html"], "lint": {"status": "skipped", "message": "No linter for .html files"}, "resolved_path": "/home/spatula/Projects/ZenNew/agent-os.html"}