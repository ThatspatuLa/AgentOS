{"success": true, "name": "agent-os-dashboard", "description": "Build and maintain an Agent OS Dashboard — a unified HTML/CSS/JS interface for monitoring AI agents, projects, sandbox activity, and token usage. Triggered when the user asks to build, update, or modify a dashboard, mission control panel, agent OS interface, token tracking, or analytics overview. Works with static HTML files (no server) that open in any browser.", "tags": [], "related_skills": [], "content": "---\nname: agent-os-dashboard\ndescription: \"Build and maintain an Agent OS Dashboard — a unified HTML/CSS/JS interface for monitoring AI agents, projects, sandbox activity, and token usage. Triggered when the user asks to build, update, or modify a dashboard, mission control panel, agent OS interface, token tracking, or analytics overview. Works with static HTML files (no server) that open in any browser.\"\n---\n\n# Agent OS Dashboard Skill\n\n## Purpose\n\nBuild a comprehensive, browser-based dashboard for monitoring and managing an AI agent ecosystem. The dashboard is a self-contained HTML file (with shared CSS) that opens in any browser — no server, no build step, no dependencies.\n\n## Design Principles\n\n1. **Dark theme, GitHub palette** — Use `#0d1117` background, `#161b22` cards, `#30363d` borders. Accent: `#58a6ff`. This matches Obsidian dark mode and is easy on the eyes for long sessions.\n2. **Sidebar + main content layout** — Fixed 220px left sidebar for navigation, scrollable main content area. Collapses to 60px icon-only on narrow screens.\n3. **Tab-based page switching** — Single HTML file with multiple `div.os-page` sections. JavaScript `showPage(id)` hides/shows pages. No routing, no reload.\n4. **Static data via JSON fetch with embedded fallback** — On load, JS fetches `agent-os-data.json` (generated by `generate-data.py`). Falls back to embedded JSON in `<script type=\"application/json\" id=\"embedded-data\">` if fetch fails. Update data by running `python3 generate-data.py` — it regenerates both the JSON file AND the embedded data in the HTML.\n5. **Color-coded agents** — Each agent gets a unique color and emoji avatar. Use colored top borders on cards (`::before` pseudo-element).\n6. **Status dots** — Green (`#3fb950`) = active, muted (`#484f58`) = idle, yellow (`#d29922`) = warning, red (`#f85149`) = error. Add `box-shadow: 0 0 6px` for glowing active dots.\n7. **No external dependencies** — Use system fonts (`Inter`, `JetBrains Mono` via Google Fonts CDN). No frameworks, no npm, no build tools.\n\n## File Structure\n\n```\nagent-os.html       — Main dashboard (all pages, all JS, embedded data)\nagent-os.css        — Shared stylesheet (all components)\nagent-os-data.json  — Generated data (output of generate-data.py)\ngenerate-data.py    — Data generator: reads Hermes logs + Obsidian vault → agent-os-data.json + updates embedded data in HTML\n```\n\nAll files live in the project directory (e.g., `~/Projects/ZenNew/`).\n\n## Page Architecture\n\nEach page is a `<div id=\"page-<name>\" class=\"os-page\">` section. Only one is visible at a time.\n\n### Required Pages\n\n1. **Dashboard (Mission Control)** — Top-level overview\n   - Agent cards grid (3 columns, one per agent; clicking opens project page)\n   - KPI stats row (4 cards: total prompts, accepted, API calls, sessions)\n   - Two-column: Activity feed (filterable) + Project status table\n\n2. **Sandbox** — Prompt pipeline and review\n   - Stats row (total, accepted, rejected, high quality)\n   - Two-column: Prompt pipeline (filterable All/Accepted/Rejected/Pending) + Promotion log\n\n3. **Project Pages** — Full page per project (opened from sidebar or agent card — NOT a side panel)\n   - Back to Dashboard button\n   - Project header with colored avatar, name, description\n   - 4 stat cards (total/accepted/rejected/pending)\n   - Two-column: Runs panel (filterable All/Accepted/Rejected) + Activity panel (filtered from run_log)\n\n4. **Memory** — Knowledge browser\n   - User profile section\n   - Environment facts section\n\n5. **Tokens** — Token usage and cost tracking (renamed from \"Spend\")\n   - 4 KPI cards (cost today, tokens today, API calls today, total cost all time)\n   - Model breakdown table: Model / Input / Output / Calls / Cost / Limit — sorted by cost descending, FREE models highlighted green\n   - **Limit column**: shows model context window size and % of limit consumed today (color-coded: green <50%, yellow 50-80%, red >80%)\n   - Cost by Model horizontal bar chart\n   - Daily Usage horizontal bar chart (7 days)\n\n6. **Income** — Earnings tracking\n   - Add Income form (source, amount, project, type) → saves to localStorage\n   - KPI cards (total earnings, monthly recurring, entry count)\n   - Income log table with type badges\n\n7. **Integrations** — External service status (accessed via gear icon popup only)\n   - Service cards: Discord, Obsidian, OpenRouter, Cron, Multi-Model Router, Local Worker\n\n8. **Run Log** — Event history (accessed via gear icon popup only)\n   - Chronological event feed with colored dots by type\n   - Filterable by type (All/Sandbox/System/Errors)\n\n## Sidebar Structure\n\nOrder top to bottom:\n1. **Main** — Dashboard, Sandbox, Memory\n2. **Insights** — Tokens, Income\n3. **Projects** — Zen, Kiyosaki, Rin, Minato, Toji, Kazuki (with colored dots and live prompt counts)\n4. **Footer** — Last updated timestamp (bottom-left) + gear icon (bottom-right of footer)\n\n- **No System nav section** — Integrations and Run Log accessible via gear popup only\n- **No auto-refresh timer** — last updated timestamp shows relative time (e.g. \"Updated 2m ago\")\n- **Gear icon** (32px, sidebar footer): popup menu with Integrations + Run Log + Refresh links. Close on outside click. Refresh should manually reload `agent-os-data.json`, rerender Zen OS, set a manual refresh timestamp so the footer immediately reads like `Updated just now`, and close the popup with brief loading/success feedback. It cannot run `generate-data.py` from a static `file://` dashboard; regenerate data separately, then use Refresh to pick up the JSON/embedded update.\n- **Project items**: clicking navigates to a full project page (not a side panel)\n- **Agent cards**: clicking also navigates to the corresponding project page\n\n## CSS Component Library\n\nDefine these reusable classes in `agent-os.css`:\n\n| Class | Purpose |\n|---|---|\n| `.os-shell` | Flex container for sidebar + main |\n| `.os-sidebar` | Fixed left navigation |\n| `.os-nav-item` | Navigation button (with `.active` state) |\n| `.os-main` | Main content area |\n| `.os-top-bar` | Page title + actions row |\n| `.os-search` | Search input |\n| `.os-btn` | Button (`.os-btn-primary`, `.os-btn-sm`) |\n| `.os-agent-grid` | 3-column agent card grid |\n| `.os-agent-card` | Individual agent card (`.zen`, `.kiyosaki`, etc.) |\n| `.os-stats-row` | 4-column stats grid |\n| `.os-stat-card` | Stat card with label, value, bar |\n| `.os-panel` | Content panel with header + body |\n| `.os-tab-bar` | Tab button group |\n| `.os-tab` | Tab button (`.active` state) |\n| `.os-two-col` | Two-column grid |\n| `.os-activity-item` | Activity feed entry with dot + line |\n| `.os-table` | Data table |\n| `.os-badge` | Status badge (`.active`, `.pending`, `.idle`, `.error`) |\n| `.os-dot` | Colored status dot (`.green`, `.yellow`, `.red`, `.muted`) |\n| `.os-progress` | Progress bar track |\n| `.os-progress-fill` | Progress bar fill |\n| `.os-mini-card` | Small card for grids |\n| `.os-cards-grid` | 3-column card grid |\n| `.os-project-item` | Sidebar project list item |\n| `.os-project-dot` | Colored dot per project |\n| `.os-project-meta` | Prompt count on project items |\n| `.os-sidebar-footer` | Footer row (timestamp + gear) |\n| `.os-last-updated` | Last updated text |\n| `.os-sidebar-gear` | Gear button container |\n| `.os-gear-btn` | Small gear button (sidebar footer) |\n| `.os-gear-popup` | Gear menu popup (`.open` state) |\n| `.os-gear-item` | Gear menu item |\n| `.os-search-results` | Search dropdown (`.open` state) |\n| `.os-search-highlight` | Highlighted search match |\n| `.income-form` | Inline income form (`.open` state) |\n| `.pipeline-row` | Pipeline table row |\n| `.pipeline-badge` | Status badge (`.accepted`, `.rejected`, `.pending`) |\n| `.runlog-filter` | Run Log filter buttons |\n| `.tokens-model-row` | Tokens page model table row (6-col grid: 2fr 1fr 1fr 1fr 1fr 1fr) |\n| `.tokens-model-header` | Tokens table header row |\n| `.tokens-bar-row` | Horizontal bar chart row |\n| `.project-back` | Back to Dashboard button |\n| `.project-header` | Project page header |\n| `.project-run-item` | Project page run entry |\n\n## JavaScript Patterns\n\n### Project Page Navigation\n```javascript\nfunction openProject(name) {\n  activeProject = name;\n  const info = projectInfo[name];\n  const proj = d.projects[name] || {total:0,accepted:0,rejected:0,pending:0};\n  const prompts = (d.sandbox.prompts||[]).filter(p => p.project === name);\n  const activity = (d.run_log||[]).filter(r => r.details.toLowerCase().includes(name.toLowerCase()));\n  // Set active state on sidebar project item\n  // Render: header, stats, filterable runs, activity feed\n  // Show page-project, hide all others\n  // Do NOT use a side panel — this is a full page\n}\nfunction filterProjectRuns(filter, btn) {\n  projectRunFilter = filter;\n  btn.closest('.os-tab-bar').querySelectorAll('.os-tab').forEach(t=>t.classList.remove('active'));\n  btn.classList.add('active');\n  renderProjectRuns(prompts);\n}\n```\n\n### Data Loading (Embedded Fallback Pattern)\n```javascript\nasync function loadData() {\n  try {\n    const r = await fetch('agent-os-data.json?t=' + Date.now());\n    DASHBOARD_DATA = await r.json();\n  } catch(e) {\n    // Fallback: read embedded JSON from HTML\n    const embedded = document.getElementById('embedded-data');\n    if (embedded) {\n      try { DASHBOARD_DATA = JSON.parse(embedded.textContent); }\n      catch(e2) { DASHBOARD_DATA = null; }\n    } else {\n      DASHBOARD_DATA = null;\n    }\n  }\n  renderAll();\n}\n// getData() returns DASHBOARD_DATA or a safe fallback object with all required keys\n```\n\n### Render Resilience\n```javascript\nfunction renderAll() {\n  const d = getData();\n  const fns = [\n    () => renderAgents(d), () => renderStats(d), () => renderActivity('all'),\n    () => renderProjectTable(d), () => renderSandbox(d), () => renderTokens(d),\n    () => renderIncome(), () => renderIntegrations(), () => renderRunLog('all'),\n    () => renderMemory(d), () => updateProjectSidebar(d), () => updateLastUpdated(),\n  ];\n  // Each render wrapped in try/catch so one failure doesn't kill the rest\n  fns.forEach(fn => { try { fn(); } catch(e) { console.error('Render error:', e); } });\n}\n```\n\n### Token Limits\n```javascript\nconst MODEL_LIMITS = {\n  \"openrouter/owl-alpha\": 128000,\n  \"gpt-5.5\": 128000,\n  \"minimax-m3:cloud\": 1000000,\n  \"nemotron-3-ultra:cloud\": 128000,\n  \"claude-opus-4.8\": 200000,\n  \"qwen3-coder:30b\": 131072,\n  \"qwen2.5-coder:14b\": 131072,\n  \"default\": 128000\n};\nfunction getModelLimit(name) { return MODEL_LIMITS[name] || MODEL_LIMITS[\"default\"]; }\n// In table: show limit and % consumed = Math.round((m.in / limit) * 100)\n// Color: <50% green, 50-80% yellow, >80% red\n```\n\n### Income Data\nIncome entries stored in `localStorage` under key `zen_income`. Load on render, save on form submit.\n\n### Gear Popup\n```javascript\nfunction toggleGear(e) {\n  if (e) e.stopPropagation();\n  document.getElementById('gear-popup').classList.toggle('open');\n}\n// Close on outside click:\ndocument.addEventListener('click', e => {\n  const p = document.getElementById('gear-popup');\n  const b = e.target.closest('.os-sidebar-gear');\n  if (p && !b) p.classList.remove('open');\n});\n```\n\n## generate-data.py — Embedded Data Update\n\nAfter writing `agent-os-data.json`, the script also updates the embedded JSON in `agent-os.html`:\n\n```python\n# Use string find/slice — NOT re.sub (JSON contains \\u escapes that break regex)\nmarker_start = '<script type=\"application/json\" id=\"embedded-data\">'\nmarker_end = '</script>'\nidx_start = html.find(marker_start)\nidx_end = html.find(marker_end, idx_start + len(marker_start))\nif idx_start > 0 and idx_end > 0:\n    new_block = marker_start + '\\n' + json.dumps(data, default=str) + '\\n' + marker_end\n    html = html[:idx_start] + new_block + html[idx_end + len(marker_end):]\n    html_path.write_text(html)\n```\n\n**Pitfall**: Do NOT use `re.sub()` to replace the embedded JSON — the JSON contains `\\u` escape sequences (e.g. `\\u2014` for em-dash) that Python's regex engine interprets as invalid unicode escapes. Use string `find()` and slice instead.\n\n## JavaScript Debugging Patterns (Agent OS Specific)\n\n### Verifying Static Content Rendering\nBrowser `browser_snapshot` only shows interactive elements (buttons, inputs, links). **It will NOT show** stat cards, table rows, bar charts, or other non-interactive rendered content. To verify these:\n\n```javascript\n// Use browser_console with getElementById to check actual content\ndocument.getElementById('tokens-stats').innerHTML.length    // > 0 means content rendered\ndocument.getElementById('tokens-by-model').innerHTML.length // > 0 means table rendered\ndocument.querySelectorAll('#tokens-by-model .tokens-model-row').length  // count rows\n```\n\n### Embedded Data Fallback\nThe dashboard uses `<script type=\"application/json\" id=\"embedded-data\">` as a fallback when `fetch()` fails on `file://` protocol. `generate-data.py` auto-updates this embedded JSON. On page load:\n1. Try `fetch('agent-os-data.json')` — works when served via HTTP\n2. If fetch fails, `JSON.parse(document.getElementById('embedded-data').textContent)` — works for `file://`\n3. If both fail, `getData()` returns a safe empty fallback object\n\n**Pitfall**: `re.sub()` breaks embedded JSON replacement — JSON contains `\\\\u` escape sequences that Python's regex engine interprets as invalid unicode escapes. Use string `find()` + slice instead.\n\n### Render Resilience\n`renderAll()` wraps each sub-render in try/catch so one failure doesn't kill the rest. A syntax error in ONE function (e.g. extra quote in template literal) breaks the entire script block and prevents `renderAll` from being defined. Always verify `typeof renderAll === 'function'` after edits.\n\n### Common Silent Failures\n- **Extra quote in template literal**: `` `text${var}'s text` `` — the `'s` creates an unterminated string that breaks the entire JS block. Use `'s':''` instead.\n- **Removing helper functions when patching**: `fmtTok()` and `fmtCost()` are defined in the `// ── Format ──` section. When replacing that section, keep the function definitions.\n- **Undefined variable in template**: Referencing an undefined variable like `${pctColor}` throws ReferenceError at render time, caught silently by try/catch.\n\n## Token Tracking Page Design\n\n### Page Structure\n1. **Token helper buttons live in the title bar** — place compact right-side buttons beside the Tokens subtitle (`Usage, costs, context limits, and model breakdown`): `⏱️ Cloud Capacity` and `📖 How to Read`. Clicking either should drop down its content under the title area. Do **not** render these helpers as full-width panels in the main page flow. Keep Cloud Capacity and How to Read visually consistent: same compact helper panel shell, same two-column helper grid, same typography/spacing.\n2. **How to read content stays narrow and mechanical** — keep exactly one collapsed helper. It should only explain token mechanics: Input tokens, Output tokens, K/M units, Calls, Context limit, and FREE/Cost wording. Do **not** add a second explanatory block defining Actual Spend/Regular Cloud/Free Tier/Local Worker; the user found that redundant.\n3. **Cloud Capacity content is a helper, not a main section** — keep it collapsed behind the title-bar button. It can show Codex/Owl refresh windows, SB rule, and tracking notes, but should not push the dashboard content down unless opened.\n4. **4 stat cards in one row**: Actual Spend Today, Local Work Completed, Estimated Cost Avoided, Cloud → Local Prompt Tokens. User explicitly prefers one clean 4-card row, not 3 + 1 wrapping.\n5. **Local Worker Economics** — focus on operational/economic metrics rather than Ollama inventory: Cloud → Local prompt tokens, estimated cost avoided, local work completed, and compact gpt-oss specs. Keep the local specs table lean: Model, Size, Params, Quant, Cost. Do **not** show a local context column or verbose Ollama endpoint/version/tracking-note prose in this section unless explicitly requested.\n6. **Token Flow Over Time** — main eye-catcher graph between Local Worker Economics and Model Routing Mix. Use a clean SVG time-series graph: Y-axis tokens, X-axis time/days, separate colored lines for each model, and a shaded/dashed total-token area behind the lines. Put a legend to the right listing each model and the shaded total-token area. Prefer controlled linear or monotone paths; avoid aggressive spline/Bezier smoothing because it can overshoot between points, create misleading humps, and disconnect the filled total-token area from actual data. The filled total area must use the same data path as the total line. Each data point should have a floating hover tooltip with model, token usage, and day, plus a subtle point-pop hover effect.\n7. **Model Routing Mix** — interactive SVG pie chart based on routed prompt/call count. Slices: Local Worker, Regular Cloud, Free Tier Cloud. Add range filters in the panel header: Intraday, Weekly, Monthly, All Time. Hover should pop a slice out and show usage amount + % of all prompts, plus a breakdown within that slice (Regular Cloud → Owl Alpha vs Codex; Free Tier Cloud → minimax-m3 vs nemotron-3-ultra). The breakdown should appear as a floating hover tooltip, not a fixed right-side box. Avoid explanatory footer text under the chart unless the user asks for it. **Single-slice pitfall:** when a selected range has only one non-zero slice (e.g. Intraday Regular Cloud only), SVG arc paths using start=end can render invisible; special-case ≥99.9% as a full circle/donut path so the pie remains visible.\n8. **Regular Cloud Models** — only Owl Alpha and Codex. These are Zen's main cloud reasoning/review/fallback models. Show tokens/calls but label cost FREE/allowance unless real paid spend is explicitly detected. Under the FREE cell, include a compact usage-vs-limit bar (`used / limit`).\n9. **Free Tier Models** — only `minimax-m3:cloud` and `nemotron-3-ultra:cloud` for current Zen setup. Label cost FREE and include the same compact `used / limit` bar under FREE.\n10. **No Paid Cloud Models section until real paid usage exists** — do not show empty paid tables or old paid-only cost charts by default.\n11. **Professional entry motion, used sparingly** — when entering the Tokens page from Dashboard/sidebar, add restrained polish: stat cards rise/fade in, KPI numbers count from 0 to final values, token graph lines draw left-to-right, total area fades in, and pie slices animate in with a subtle circular/scale feel. Keep animations short and purposeful; do not animate every component or make data harder to read.\n\n### Model Categorization\n```javascript\nconst regularCloud = ['openrouter/owl-alpha', 'openai-codex'];\nconst freeTierCloud = ['ollama/minimax-m3:cloud', 'ollama/nemotron-3-ultra:cloud'];\nconst localWorker = ['ollama/gpt-oss:20b'];\n// Hide every other cloud model from the Tokens UI unless the user explicitly asks to surface paid/legacy usage.\n```\n\nSee `references/token-section-redesign.md` for the June 2026 design decisions, cost-avoided baseline, and verification checklist.\n\n### Context Limit Column\nShows `used / limit` with a small progress bar underneath. Color: green (<60%), yellow (60-90%), red (>90%). The limit is the model's context window size (e.g. 128K for owl-alpha, 200K for claude-opus, 1M for minimax-m3). Note: showing % consumed is misleading for cumulative daily totals vs per-request context windows — the bar is a visual reference of total usage vs model capacity.\n\n### Model Limits Map\n```javascript\nconst MODEL_LIMITS = {\n  \"openrouter/owl-alpha\": 128000,\n  \"gpt-5.5\": 128000,\n  \"minimax-m3:cloud\": 1000000,\n  \"nemotron-3-ultra:cloud\": 128000,\n  \"claude-opus-4.8\": 200000,\n  \"qwen3-coder:30b\": 131072,\n  \"default\": 128000\n}\n```\n\n### Pagination Embedding via generate-data.py\n`generate-data.py` writes `agent-os-data.json` AND updates the embedded JSON in `agent-os.html` using string find/slice (NOT regex). Run after any log changes to keep dashboard current.\n\n## Responsive Breakpoints\n\n- `≤1200px`: Agent grid → 3 cols, stats → 2 cols, cards → 2 cols\n- `≤900px`: Sidebar collapses to 60px icons-only (hides labels, meta, timestamp, gear), two-col → 1 col\n\n## Color Per Agent\n\n| Agent | Variable | Hex |\n|---|---|---|\n| Zen | `--accent` | `#58a6ff` |\n| Kiyosaki | `--green` | `#3fb950` |\n| Toji | `--yellow` | `#d29922` |\n| Rin | `--purple` | `#bc8cff` |\n| Minato | `--cyan` | `#56d4dd` |\n| Kazuki | `--pink` | `#f778ba` |\n\n## Data Pipeline\n\n```\ngenerate-data.py → agent-os-data.json + updates embedded data in agent-os.html\n                → HTML fetches JSON on load (falls back to embedded)\n```\n\nJSON structure: `token_usage` (today/yesterday/total_in/total_out/total_calls/per_model/per_session), `sandbox` (total/accepted/rejected/pending/needs_review/high_quality/prompts[]), `projects` (per-project stats), `promotions[]`, `run_log[]`, `income`.\n\nSandbox prompt fields: `id`, `summary`, `project`, `quality`, `status`, `review_status`, `task_type`, and optionally `path`. **Always use string fields** (not boolean `p.accepted`/`p.rejected`) for status checks — the real data from generate-data.py uses string status. Map `status: pending` to Pending, and map completed items with `review_status: pending` to Needs Review.\n\nSee `references/sandbox-count-consistency.md` when Sandbox stats, Prompt Pipeline rows, Obsidian Prompt Index, or flashcards disagree.\n\n## Multi-LLM Pipeline Integration\n\nThe dashboard tracks token usage from the multi-LLM pipeline. See `multi-llm-pipeline` skill for full details.\n\n**Confirmed working configuration (June 2026):**\n- **Local:** Aider + Ollama gpt-oss:20b — 78s avg, excellent quality, only model on disk (13GB)\n- **Cloud:** Owl Alpha via OpenRouter (primary), Codex (fallback)\n- **Incompatible models (removed):** deepseek-coder-v2, codellama, qwen3-coder (too slow), OpenCode (broken server-side)\n\n### Token Tracking UI Details\n\nThe Tokens page mirrors OpenRouter's activity dashboard layout. See `references/debugging-notes.md` for browser debugging patterns specific to this dashboard. See `references/token-chart-ux-patterns.md` for refresh timestamp behavior, helper dropdown consistency, non-distorting token chart rendering, and restrained Tokens page motion.\n\n**Pitfalls:**\n- `browser_snapshot` only shows interactive elements — use `browser_console` + `getElementById().innerHTML` to verify static content rendering\n- Single-character JS syntax errors (e.g. extra quote in template literal) break the entire script block\n- `generate-data.py` uses string `find()`/`slice` (NOT `re.sub()`) for embedded JSON replacement — `\\u` escapes break Python regex\n- `renderAll()` wraps each sub-render in try/catch — one failure doesn't kill the rest, but a script-level syntax error prevents `renderAll` from being defined\n- Sandbox count mismatches: if stats count all records, do not slice `sandbox.prompts` to the last 50 unless the UI explicitly says \"recent\". Keep `pending` separate from `needs_review`: generated prompts are `status: pending` + `review_status: not_started`; completed outputs awaiting user review are `review_status: pending`.\n- When patching, verify helper functions (`fmtTok`, `fmtCost`) referenced in template strings aren't accidentally removed", "path": "productivity/agent-os-dashboard/SKILL.md", "skill_dir": "/home/spatula/.hermes/skills/productivity/agent-os-dashboard", "linked_files": {"references": ["references/debugging-notes.md", "references/sandbox-count-consistency.md", "references/token-chart-ux-patterns.md", "references/token-section-redesign.md"]}, "usage_hint": "To view linked files, call skill_view(name, file_path) where file_path is e.g. 'references/api.md' or 'assets/config.yaml'", "required_environment_variables": [], "required_commands": [], "missing_required_environment_variables": [], "missing_credential_files": [], "missing_required_commands": [], "setup_needed": false, "setup_skipped": false, "readiness_status": "available"}