{"success": true, "name": "multi-llm-pipeline", "description": "Automatic multi-LLM pipeline: Zen (Owl Alpha/Codex) for thinking/research/review, local Aider+Ollama for all file mutations. Auto-routes every task — no user decisions needed. Triggered by ANY user message that produces or modifies files.", "tags": [], "related_skills": [], "content": "---\nname: multi-llm-pipeline\ndescription: \"Automatic multi-LLM pipeline: Zen (Owl Alpha/Codex) for thinking/research/review, local Aider+Ollama for all file mutations. Auto-routes every task — no user decisions needed. Triggered by ANY user message that produces or modifies files.\"\n---\n\n# Multi-LLM Pipeline — Token-Aware Auto Routing\n\n## Worker Policy — Local Aider Only\n\nZen worker execution is **Aider + local Ollama only**.\n\n- Approved worker path: `zen-worker .` (guarded wrapper around `aider --model ollama/gpt-oss:20b --no-auto-commit --yes .`; details and smoke checks live in the Aider skill's `references/zen-worker-wrapper.md`)\n- Approved local model: `ollama/gpt-oss:20b`\n- Approved local endpoint: `http://127.0.0.1:11434`\n- Main cloud models, including Owl Alpha and Codex, are for planning, discussion, review, and fallback intervention only.\n- Do **not** use Hermes `delegate_task` for Zen build/edit workers unless delegation has first been explicitly reconfigured to local Ollama and verified with a harmless test.\n- Current Hermes `delegation.*` config may inherit cloud model behavior when blank, so treat `delegate_task` as cloud-risky by default.\n\n## Sandbox Exception — Local-Only Execution Batches\n\nWhen Six asks to **run SB** / **run sandbox**, pending sandbox prompts are executed local-only:\n\n- Cloud models may have created the prompts before the run.\n- During the SB execution batch, do **not** call Owl Alpha/Codex for worker execution, text-only prompt execution, judging, or tweak loops.\n- Use `zen-worker` → Aider → local Ollama `gpt-oss:20b` plus local verification commands.\n- After all pending prompts complete, write/leave a gate note such as: `25/25 pending have been completed; please run by Cloud for review.`\n- Only then may Owl Alpha/Codex review the completed `review_status: pending` outputs as a batch.\n- Exception: explicit user-requested expiry/capacity checkpoint for cloud model tracking.\n\n## Core Principle\n\nEvery task sent to Hermes is automatically routed to the correct model. **You never need to specify which model to use.** The pipeline decides based on task type.\n\n## Model Roles (Fixed)\n\n| Role | Model | API | Cost Token? |\n|------|-------|-----|-------------|\n| **Thinker/Reviewer** | Owl Alpha | `openrouter/owl-alpha` | Yes (cloud) |\n| **Fallback Thinker** | Codex | `openai-codex` | Yes (cloud) |\n| **Local Worker** | gpt-oss:20b | `ollama/gpt-oss:20b` at `http://127.0.0.1:11434` | **No** (free) |\n| **Local Fallback** | *(none currently)* | — | — |\n\n**Default Aider model is `ollama/gpt-oss:20b`**. Tested June 2026: gpt-oss:20b is the fastest and highest quality Aider model (78s, 381 lines, all features). devstral-small-2:24b tested at 149s/254 lines — slower, removed. All other local models removed from disk — slower, incompatible, or both.\n\n## Automatic Routing Decision Tree\n\nFor EVERY user message, Zen applies this decision tree:\n\n```\nIs the task asking to CREATE or MODIFY files/artifacts?\n├── YES → LOCAL (Aider + Ollama gpt-oss:20b)\n│         Zen writes prompt → Aider executes → Zen (Owl Alpha) reviews\n│         Token cost: 0 for execution, minimal for review\n│\n└── NO → Is it RESEARCH / DISCUSSION / PLANNING / REVIEW?\n          ├── YES → CLOUD (Owl Alpha, fallback to Codex)\n          │         Zen handles directly\n          │         Token cost: full cloud usage\n          │\n          └── NO → Is it a simple query / status check?\n                    └── YES → CLOUD (Owl Alpha, lightweight)\n```\n\n## Local Worker Tasks (Aider + Ollama)\n\nALWAYS use local for:\n- `write_file` / `patch` / `execute_code` that produces artifacts\n- Building HTML/CSS/JS (dashboards, UIs, pages)\n- Writing Python / scripts / configs\n- Creating / modifying markdown docs\n- File generation of any kind\n\n**Default local model: `ollama/gpt-oss:20b`**. If gpt-oss:20b OOMs on large tasks, Zen intervenes directly with cloud model.\n\n## Cloud Tasks (Owl Alpha → Codex)\n\nALWAYS use cloud for:\n- Research, summarization, discussion\n- Planning, architecture decisions\n- Code REVIEW and quality assessment\n- Security analysis\n- Memory / skill updates\n- Tight feedback: \"tell me about X\", \"what do you think about Y\"\n- Aider output review (Stage 3 of pipeline)\n\n## Manual Override\n\nUser can override the automatic routing:\n- **\"use local\"** or **\"use Aider\"** → force Aider + Ollama even for thinking tasks\n- **\"use cloud\"** or **\"use Owl/Codex\"** → force cloud even for file mutations\n- Default is always automatic — override only when explicitly requested\n\n## Pipeline Stages (Local Tasks)\n\n### Stage 1: Zen Plans (Cloud — Owl Alpha)\nAnalyze the task. Write a self-contained prompt for Aider including:\n- Exact file paths (absolute, e.g., `/home/spatula/Projects/ZenNew/agent-os.html`)\n- Complete requirements (no ambiguity)\n- Verification command (e.g., `python3 -c \"import html; ...\"`)\n\n### Stage 2: Aider Executes (Local — Ollama)\n```bash\ncd /path/to/project && \\\nzen-worker . << 'AIDER_TASK'\n<Stage 1 prompt>\nAIDER_TASK\n```\n`zen-worker` must invoke Aider with `--message-file` and pass referenced existing files as file args; stdin-only redirection can launch Aider but silently make no edits. Always verify actual file diffs/outputs, not just exit code/log entries. See the Aider skill reference `references/local-worker-aider-audit.md` for regression tests and failure-mode details.\n**Timeout:** 10 minutes per attempt. For tasks with 5+ files, split into per-file prompts and run sequentially.\n\n### Stage 2b: Task Splitting (Recommended for 5+ files)\nInstead of one large prompt, run Aider once per file:\n```bash\ncd /path/to/project && \\\nzen-worker . << 'AIDER_TASK'\nCreate only deck.py with: Deck class, Card class (front, back, ease_factor, interval, repetitions), save/load JSON methods. Type hints and docstrings required.\nAIDER_TASK\n\ncd /path/to/project && \\\nzen-worker . << 'AIDER_TASK'\nCreate only review.py with: ReviewSession class, SM-2 spaced repetition, interactive rating loop. Type hints and docstrings required.\nAIDER_TASK\n```\nThis is faster and produces higher quality output than one massive prompt.\n\n### Stage 3: Cloud Review (Owl Alpha)\nFor normal local tasks, Zen reviews Aider's output:\n1. Read all created/modified files\n2. Run verification commands\n3. Check every requirement from Stage 1\n4. Verdict: **PASS** or **FAIL + specific issues**\n\n**SB exception:** during `run SB` batches, do not call Owl Alpha/Codex for item-by-item review. Use local verification only, then leave the completion gate note and wait for explicit cloud-review instruction.\n\n### Stage 4: Auto-Tweak Loop (Max 3)\nIf FAIL:\n- Zen writes numbered tweak prompt → Aider re-runs → Re-review\n- Escalate to direct Zen intervention after 3 failures\n- Last resort: Zen does it directly with cloud model\n\n### Stage 5: Promote\nPASS → report results, note any caveats.\n\n## Token Leak Prevention\n\n**Hard rules — never violated:**\n\n1. Aider runs ONLY with `--model ollama/gpt-oss:20b` (default)\n2. Aider NEVER receives cloud API keys — it connects to `http://127.0.0.1:11434` only\n3. Cloud models NEVER process raw file generation — only review/tweak prompts\n4. Tweak prompts contain issue descriptions, NOT cloud credentials\n5. `OLLAMA_BASE_URL` / `OPENROUTER_API_KEY` / `OPENAI_API_KEY` are NEVER passed to Aider subprocesses\n\n## Cloud Capacity Refresh Tracking\n\nTrack Owl Alpha/OpenRouter and Codex exhaustion/reset windows separately from normal token accounting. See `references/cloud-capacity-refresh-tracking.md` for the current Codex 5-hour/weekly observed reset notes, OpenRouter midnight-UTC free reset evidence, and the exhaustion-baseline method.\n\nFor Agent OS Tokens implementation details — including `cloud-refresh-baselines.json`, OpenRouter midnight-UTC → 10:00 AEST display, and local Ollama `/api/version` + `/api/tags` + `/api/show` metadata wiring — see `references/agent-os-token-capacity-ollama.md`.\n\n## Token Tracking\n\n`/home/spatula/.local/bin/zen-worker` should launch Aider with local Ollama and log every local execution. Required log targets:\n\n- `~/.hermes/sandbox/local-worker-usage.json`\n- `~/.hermes/sandbox/token-usage.json`\n\nEach wrapper run should capture stdin prompt and Aider stdout/stderr, estimate tokens conservatively as `round(chars / 4)`, write a `runs[]` entry, and update `totals[\"ollama/gpt-oss:20b\"]`. Agent OS reads these logs for Intraday/Weekly/Monthly/All Time local-worker counts and Token Flow local lines. If local dashboard work is not appearing under Local Worker, inspect the wrapper logging before assuming the local model failed. Also verify actual file diffs after every `zen-worker` run: Aider can return `pass` and log tokens even when no file changed, especially for tiny exact HTML/JS replacement prompts; in that case retry with a narrower prompt or patch directly and report the worker miss clearly.\n\nEvery pipeline run should log to `~/.hermes/sandbox/token-usage.json`:\n```json\n{\n  \"runs\": [\n    {\n      \"timestamp\": \"2026-06-09T12:00:00Z\",\n      \"task\": \"Build todo CLI\",\n      \"stage\": \"local-execution\",\n      \"model\": \"ollama/gpt-oss:20b\",\n      \"tokens_in\": 770,\n      \"tokens_out\": 3200,\n      \"cost\": 0,\n      \"result\": \"pass\",\n      \"retries\": 0\n    },\n    {\n      \"timestamp\": \"2026-06-09T12:02:00Z\",\n      \"task\": \"Build todo CLI\",\n      \"stage\": \"cloud-review\",\n      \"model\": \"openrouter/owl-alpha\",\n      \"tokens_in\": 5000,\n      \"tokens_out\": 800,\n      \"cost\": 0.02,\n      \"result\": \"pass\",\n      \"retries\": 0\n    }\n  ],\n  \"totals\": {\n    \"openrouter/owl-alpha\": {\"in\": 0, \"out\": 0, \"cost\": 0},\n    \"ollama/gpt-oss:20b\": {\"in\": 0, \"out\": 0, \"cost\": 0},\n    \"openai-codex\": {\"in\": 0, \"out\": 0, \"cost\": 0}\n  }\n}\n```\n\n## Agent OS Dashboard Integration\n\nDashboard (`~/Projects/ZenNew/agent-os.html`) displays:\n- Per-model token usage (in/out/cost/calls) — from token-usage.json + Hermes logs\n- Pipeline status indicator (which stage is active)\n- Active model badge (Owl Alpha / Codex / Ollama)\n- Smart auto-refresh (15-min timer, pauses if Zen is mid-task)\n- Cloud vs local token split (pie chart)\n\nSee `aider/references/model-test-results-2026-06-09.md` for full performance benchmarks.\n\n## User Preferences (Embedded)\n\n- **Automatic routing by default**, manual override available (\"use local\" / \"use cloud\")\n- **Owl Alpha first** for cloud, Codex as fallback when limits hit\n- **Max 3 tweak loops** before Zen intervenes manually\n- **Token tracking is mandatory** — every pipeline run must log to token-usage.json\n- **All buttons must be functional** — no placeholder UI elements\n- **Dashboard refresh only when idle** — pauses if Zen is mid-task\n- **Confirm before launching long test sequences** — when running multiple parallel tests or downloads, present the plan and wait for user confirmation before executing\n\n## Tool Comparison & Availability (Tested June 2026)\n\n| Tool | Ollama Support | Status | Notes |\n|------|---------------|--------|-------|\n| **Aider** | ✅ Native | **Working** | `--model ollama/gpt-oss:20b` (default). Fastest + best quality. devstral-small-2:24b untested. All other local models removed. ~90% correct first pass. Review stage compensates. Split prompts for 5+ files. |\n| **OpenCode** | ❌ Broken | **Unavailable** | Server-side UnknownError on ALL requests regardless of model. Cannot use. Re-test periodically as this is a platform issue. |\n| **Pi** | ⚠️ Manual | **Not installable** | npm package is a placeholder (not the real tool). Distributed via GitHub source. Skip until proper install path exists. |\n\n**Current recommendation:** Use Aider + Ollama gpt-oss:20b as the default local worker. No local fallback currently configured — if gpt-oss OOMs, Zen intervenes directly with cloud model.\n\n- **gpt-oss:20b is the DEFAULT** — 78s, 381 lines, type hints, docstrings, error handling, all features working.\n- **devstral-small-2:24b tested** — 149s, 254 lines, works but slower. Removed from disk.\n- **deepseek-coder-v2:16b is INCOMPATIBLE** — \"LLM did not conform to edit format\" errors. Removed.\n- **qwen3-coder:30b is very slow** — 5+ min per file, keeps re-editing. Removed.\n- **qwen2.5-coder:14b was good** — 3.5min, clean output. Removed (outperformed by gpt-oss:20b).\n- **codellama:13b is incompatible** — doesn't create .py files properly. Removed.\n- Aider creates artifact files (e.g., `python script.py` as literal filename) — clean up after\n- Aider may skip type hints/docstrings — review stage catches it\n- Aider ignores `--no-git` in some versions — clean up `.git` dirs after\n- Always use heredoc for Aider prompts, never pipe\n- Don't skip verification even if output looks correct\n\n## Code Generation for QuantConnect Lean\n\nWhen generating Lean Python code for trading strategies:\n\n**Parameter substitution:** Use `str.replace()` NOT f-strings with `{p['key']}`. F-string expressions like `max(p['a'], p['b'])` fail because `p` doesn't exist in the generated code.\n\n```python\ndef sub(code):\n    for key, val in p.items():\n        code = code.replace('{' + key + '}', str(val))\n    return code\n```\n\n**QuantConnect API constraints:**\n- `KeltnerChannels` does NOT exist — use `self.BB()` as a substitute\n- `RollingWindow.add(value)` — NOT `.append()`\n- `BB` has `.upper_band`, `.lower_band`, `.middle_band`, `.is_ready`\n\nSee `trading-system-development/references/lean-code-generation-patterns.md` for full details.\n\nAfter every run:\n1. All requested files exist at correct paths\n2. Verification commands pass\n3. No artifact files left behind\n4. Token usage logged\n", "path": "autonomous-ai-agents/multi-llm-pipeline/SKILL.md", "skill_dir": "/home/spatula/.hermes/skills/autonomous-ai-agents/multi-llm-pipeline", "linked_files": {"references": ["references/test-results-2026-06-09.md", "references/cloud-capacity-refresh-tracking.md", "references/agent-os-token-capacity-ollama.md"]}, "usage_hint": "To view linked files, call skill_view(name, file_path) where file_path is e.g. 'references/api.md' or 'assets/config.yaml'", "required_environment_variables": [], "required_commands": [], "missing_required_environment_variables": [], "missing_credential_files": [], "missing_required_commands": [], "setup_needed": false, "setup_skipped": false, "readiness_status": "available"}